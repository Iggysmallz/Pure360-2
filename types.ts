
export interface Service {
  id: string;
  title: string;
  description: string;
  features: string[];
  price: string;
  imageUrl: string;
}

export interface TeamMember {
  name: string;
  quote: string;
  imageUrl: string;
}
